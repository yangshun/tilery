---
title: Floating Panels
description: Detach panels into movable overlays and dock them back.
category: Advanced UI
---

import { Demo } from '../../../components/demo';
import {
  InitialFloatingExample,
  RuntimeFloatingExample,
  TabFloatingExample,
  NativePopoutExample,
  PopoutStylingExample,
} from './example';

Floating panels let users pull part of the workspace out of the tiled layout while keeping it in the same Tilery state. Move and resize a floating panel, then dock it back into the main layout.

This page also shows the difference between floating a whole panel, floating one tab, and opening a native popout window with copied document-head styles.

<Demo
  source="floating/example.tsx"
  regions={['initial-floating-layout', 'initial-floating-tilery']}
  surface="plain"
  height={800}>
  <InitialFloatingExample />
</Demo>

<Demo
  source="floating/example.tsx"
  regions={[
    'runtime-floating-layout',
    'runtime-floating-controller',
    'runtime-floating-tilery',
  ]}
  surface="plain"
  height={800}>
  <RuntimeFloatingExample />
</Demo>

<Demo
  source="floating/example.tsx"
  regions={[
    'tab-floating-layout',
    'tab-floating-controller',
    'tab-floating-tilery',
  ]}
  surface="plain"
  height={800}>
  <TabFloatingExample />
</Demo>

<Demo
  source="floating/example.tsx"
  regions={[
    'runtime-floating-layout',
    'native-popout-controller',
    'native-popout-tilery',
  ]}
  surface="plain"
  height={800}>
  <NativePopoutExample />
</Demo>

<Demo
  source="floating/example.tsx"
  regions={[
    'popout-styling-layout',
    'popout-styling-theme',
    'popout-styling-controller',
    'popout-styling-tilery',
  ]}
  surface="plain"
  height={800}>
  <PopoutStylingExample />
</Demo>

## Related

- [Programmatic Control](/docs/api/control) — Use float, dock, popout, and bounds APIs.
- [Styling](/docs/styling) — Make native popout styles survive the separate document.
